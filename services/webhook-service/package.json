{
  "name": "webhook-service",
  "version": "0.1.0",
  "private": true,
  "description": "Receives and validates payment provider webhooks.",
  "main": "src/handler.js",
  "scripts": {
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --runInBand"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.883.0",
    "@aws-sdk/client-ssm": "^3.883.0",
    "@aws-sdk/client-sns": "^3.883.0",
    "express": "^5.1.0",
    "serverless-http": "^3.2.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "serverless": "^4.18.1",
    "serverless-offline": "^14.4.0",
    "supertest": "^7.1.4"
  }
}
