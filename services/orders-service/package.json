{
  "name": "orders-service",
  "version": "0.1.0",
  "private": true,
  "description": "Receives payment orders and publishes payment events to SQS.",
  "main": "src/handler.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --runInBand"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.883.0",
    "express": "^5.1.0",
    "serverless-http": "^3.2.0",
    "zod": "^4.1.5",
    "nodemon": "^3.1.14"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "serverless": "^4.18.1",
    "serverless-offline": "^14.4.0",
    "supertest": "^7.1.4"
  }
}